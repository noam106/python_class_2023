import datetime
if __name__ == '__main__':
    def date_count(date_to: datetime.date.day) -> int:
        courent_mount = date_to.datetime.month
        x = courent_mount
        while courent_mount == x:
            for day in date_to:
                yield (day)
